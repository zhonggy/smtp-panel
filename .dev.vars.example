# 复制为 .dev.vars 用于本地开发(已被 .gitignore 忽略)
# 生产环境请使用: npx wrangler secret put ENCRYPTION_KEY
#
# 用于加密 SMTP 密码与外部 API Key(AES-256-GCM)
# 请填写一段随机的长字符串(建议 32 位以上,如 openssl rand -base64 32 的输出)
# 留空或不填时,系统会自动生成随机密钥并存储在数据库中(可通过 Secret 覆盖)
ENCRYPTION_KEY=
