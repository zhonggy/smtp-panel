# 复制为 .env.deploy 并填入真实值(该文件已被 .gitignore 忽略)
#
# 这些是 Cloudflare 资源标识符,不是密钥;此处外置仅为保持仓库整洁,
# 让任何人都能克隆后绑定到自己的资源。
#
# 获取方式:
#   npx wrangler d1 list             → 找到 smtp-panel 的 uuid
#   npx wrangler kv namespace list   → 找到 KV 的 id
#
# 若资源尚未创建:
#   npx wrangler d1 create smtp-panel
#   npx wrangler kv namespace create KV
#   npx wrangler queues create smtp-panel-mail
#
# CI / Workers Builds 环境下改为设置同名环境变量即可,无需此文件。

D1_DATABASE_ID=
KV_NAMESPACE_ID=
