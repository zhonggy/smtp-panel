# 复制为 .env.deploy 并填入真实值(该文件已被 .gitignore 忽略)
#
# 大多数情况下你不需要这个文件:
#   · 一键部署  —— Cloudflare 自动创建资源并写入配置,零配置
#   · 本地部署  —— 先 npx wrangler login,脚本会自动发现或创建资源,
#                  并把 ID 记录到 .env.deploy
#
# 仅在下列场景需要手动填写:
#   · 想绑定到指定的已有资源(而非按名称匹配到的那个)
#   · Workers Builds 构建失败提示无法解析资源 ID
#     (此时也可改为在 Dashboard → Worker → Settings → Build → Variables 添加同名变量)
#
# 查询已有资源:
#   npx wrangler d1 list
#   npx wrangler kv namespace list

D1_DATABASE_ID=
KV_NAMESPACE_ID=
