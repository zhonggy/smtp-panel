# 复制为 .dev.vars 用于本地开发(已被 .gitignore 忽略)
# 生产环境请使用: npx wrangler secret put ENCRYPTION_KEY --config apps/worker/wrangler.toml
#
# 用于加密 SMTP 密码与外部 API Key(AES-256-GCM),至少 32 个随机字符
ENCRYPTION_KEY=change-me-to-a-random-string-32-chars-min!
